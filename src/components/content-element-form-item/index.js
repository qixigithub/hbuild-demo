import ContentElementFormItem from './src/main';

ContentElementFormItem.install = function(Vue) {
  Vue.component(ContentElementFormItem.name, ContentElementFormItem);
};

export default ContentElementFormItem;

